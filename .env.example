# Copy this file to .env and adjust values. .env is gitignored; .env.example is tracked.

# Server
PORT=3050
HOST=127.0.0.1
MAX_ROOM_SIZE=10
MAX_ROOMS=1000
FRAME_KB=16
LOG_LEVEL=info

# Extension (used at build time to embed default server URL)
DEFAULT_SERVER_URL=ws://localhost:3050
